spring:
  application:
    name: demo

    datasource:
      url: jdbc:h2:mem:product
      username: sa
      password: teste
      driver-class-name: org.h2.Driver
    jpa:
      database-plataform: org.hibernate.dialect.H2Dialect
      hibernate:
        dll=auto: update
    h2:
      console:
        enabled: true
        path: /h2-console

  api:
    security:
      token:
        secret=my-secrety-key-from-v

serve:
  port: 8080
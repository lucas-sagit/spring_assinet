spring:
  datasource:
    url: jdbc:h2:mem:assinet
    driver-class-name: org.h2.Driver
    username: sa
    password: teste
  h2:
    console:
      enabled: true
      path: /h2-console

#  server:
#    port: 8080

api:
  security:
    token:
      secret: 
